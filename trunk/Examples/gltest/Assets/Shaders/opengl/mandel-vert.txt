// mandel-vert.txt

uniform float gTime; 
uniform float mouseX;
uniform float mouseY;
varying vec2 vUv;

void main()
{
  gl_Position = ftransform(); 
  vec2 m = vec2(mouseX, mouseY);
  vUv = gl_MultiTexCoord0.xy / gTime - m;
}
