// mandel-vert.txt

uniform float gTime; 
uniform float mouseX;
uniform float mouseY;
varying vec2 vUv;

void main()
{
  gl_Position = ftransform(); 
  vUv = (gl_MultiTexCoord0.xy - vec2(mouseX, mouseY)) / gTime;
}
